# Production environment variables
# These values are used when deploying to Cloudflare Pages

# Cloudflare Workers Backend URL (Single Worker Mode)
# Uses single backend worker - file splitting handled by client
VITE_API_URL=https://siem-backend.tanubhavj.workers.dev
